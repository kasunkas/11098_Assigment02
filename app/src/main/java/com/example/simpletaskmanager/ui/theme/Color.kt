package com.example.simpletaskmanager.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Light theme palette ──────────────────────────────────────────────────────
val Indigo40        = Color(0xFF3949AB)   // Primary – deep indigo
val IndigoVariant40 = Color(0xFF5C6BC0)   // Secondary – medium indigo
val Teal40          = Color(0xFF00838F)   // Tertiary – teal accent

val LightBackground = Color(0xFFF4F6FB)   // Soft off-white background
val LightSurface    = Color(0xFFFFFFFF)   // Card/dialog surface
val LightSurfaceVar = Color(0xFFE8EAF6)   // Chip / secondary surface

// ─── Dark theme palette ───────────────────────────────────────────────────────
val Indigo80        = Color(0xFF9FA8DA)   // Primary – lavender indigo
val IndigoVariant80 = Color(0xFFB0BEC5)   // Secondary – muted blue-grey
val Teal80          = Color(0xFF80DEEA)   // Tertiary – light teal

val DarkBackground  = Color(0xFF0F1117)   // Deep dark background
val DarkSurface     = Color(0xFF1A1D27)   // Elevated card surface
val DarkSurfaceVar  = Color(0xFF252836)   // Chip / secondary surface

// ─── Priority colours (shared across themes) ──────────────────────────────────
val PriorityHigh   = Color(0xFFEF5350)   // Red
val PriorityMedium = Color(0xFFFFB300)   // Amber
val PriorityLow    = Color(0xFF66BB6A)   // Green
